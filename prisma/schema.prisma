generator client {
  provider = "prisma-client"
  output   = "../generated/prisma"
}

datasource db {
  provider = "sqlite"
}

model ChatSession {
  id        String   @id
  path      String
  messages  Json
  createdAt DateTime
  updatedAt DateTime
}
